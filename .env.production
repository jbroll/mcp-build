# MCP Build Service Production Environment Variables
# This file will be deployed to /etc/mcp-build/secrets.env on the remote server
# and loaded by the systemd service.

# Transport mode: http for remote access via Apache proxy
MCP_BUILD_TRANSPORT=http

# Bind to localhost since Apache handles external connections
MCP_BUILD_HOST=127.0.0.1
MCP_BUILD_PORT=3344

# Session key for authentication
# IMPORTANT: Generate a secure random key with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Replace this value before deploying!
MCP_BUILD_SESSION_KEY=CHANGE_ME_TO_SECURE_RANDOM_KEY

# Directory containing repositories to build
# This should be created on the remote server before deployment:
#   sudo mkdir -p /var/lib/mcp-build/repos
#   sudo chown mcp-build:mcp-build /var/lib/mcp-build/repos
MCP_BUILD_REPOS_DIR=/var/lib/mcp-build/repos
